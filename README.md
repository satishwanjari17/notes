# notes
this project requerd nodejs and mongodb with latest version 
steps to run the project :- 
1) start the mongodb
2) clone the project and open terminal in that project
3) run the command calle node app.js
4) got to the browser and paste "www.localhost:8080"

